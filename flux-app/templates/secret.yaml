apiVersion: v1
stringData:
  values.yaml: |
    {{- .Values.application.values | nindent 4 }}
kind: Secret
metadata:
  name: bigbang-{{- .Values.application.name -}}-values
  namespace: bigbang
type: generic
