# hostname is deprecated and replaced with domain. But if hostname exists then use it.
hostname: bigbang.dev
domain: bigbang.dev

# Define variables to help with conditionals later

openshift: false

istio:
  enabled: true
  injection: enabled
  gitlab:
    gateways:
    - istio-system/tenant
  registry:
    gateways:
    - istio-system/tenant

monitoring:
  enabled: true

networkPolicies:
  enabled: true
  ingressLabels:
    app: tenant-ingressgateway
    istio: null
  controlPlaneCidr: 0.0.0.0/0
redis:
  metrics:
    serviceMonitor:
      enabled: true
      namespace: gitlab
  master:
    podAnnotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
  slave:
    podAnnotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
postgresql:
  master:
    podAnnotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
  slave:
    podAnnotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
registry:
  annotations:
  # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
    bigbang.dev/istioVersion: 1.17.3
  metrics:
    serviceMonitor:
      endpointConfig:
        scheme: https
        tlsConfig:
          caFile: /etc/prom-certs/root-cert.pem
          certFile: /etc/prom-certs/cert-chain.pem
          keyFile: /etc/prom-certs/key.pem
          insecureSkipVerify: true  # Prometheus does not support Istio security naming, thus skip verifying target pod certificate

gitlab:
  toolbox:
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
  gitlab-exporter:
    enabled: true
    metrics:
      annotations:
      # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
        bigbang.dev/istioVersion: 1.17.3
      serviceMonitor:
        endpointConfig:
          scheme: https
          tlsConfig:
            caFile: /etc/prom-certs/root-cert.pem
            certFile: /etc/prom-certs/cert-chain.pem
            keyFile: /etc/prom-certs/key.pem
            insecureSkipVerify: true  # Prometheus does not support Istio security naming, thus skip verifying target pod certificate
  webservice:
    metrics:
      serviceMonitor:
        endpointConfig:
          scheme: https
          tlsConfig:
            caFile: /etc/prom-certs/root-cert.pem
            certFile: /etc/prom-certs/cert-chain.pem
            keyFile: /etc/prom-certs/key.pem
            insecureSkipVerify: true  # Prometheus does not support Istio security naming, thus skip verifying target pod certificate
    workhorse:
      metrics:
        serviceMonitor:
          endpointConfig:
            scheme: https
            tlsConfig:
              caFile: /etc/prom-certs/root-cert.pem
              certFile: /etc/prom-certs/cert-chain.pem
              keyFile: /etc/prom-certs/key.pem
              insecureSkipVerify: true  # Prometheus does not support Istio security naming, thus skip verifying target pod certificate
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
  sidekiq:
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
  migrations:
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
  gitaly:
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
    metrics:
      serviceMonitor:
        endpointConfig:
          scheme: https
          tlsConfig:
            caFile: /etc/prom-certs/root-cert.pem
            certFile: /etc/prom-certs/cert-chain.pem
            keyFile: /etc/prom-certs/key.pem
            insecureSkipVerify: true  # Prometheus does not support Istio security naming, thus skip verifying target pod certificate
  gitlab-shell:
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
    metrics:
      serviceMonitor:
        endpointConfig:
          scheme: https
          tlsConfig:
            caFile: /etc/prom-certs/root-cert.pem
            certFile: /etc/prom-certs/cert-chain.pem
            keyFile: /etc/prom-certs/key.pem
            insecureSkipVerify: true  # Prometheus does not support Istio security naming, thus skip verifying target pod certificate

  praefect:
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
  gitlab-grafana:
    annotations:
    # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
      bigbang.dev/istioVersion: 1.17.3
shared-secrets:
  annotations:
  # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
    bigbang.dev/istioVersion: 1.17.3
minio:
  podAnnotations:
  # renovate: datasource=gitlab-tags depName=big-bang/product/packages/istio-controlplane versioning=loose extractVersion=^(?<version>\d+\.\d+\.\d+) registryUrl=https://repo1.dso.mil
    bigbang.dev/istioVersion: 1.17.3

global:

  # added to help with Gitlab sub-chart configuration
  image:
    pullPolicy: IfNotPresent

  istio:
    enabled: true
    injection: enabled

  hosts:
    domain: bigbang.dev

    gitlab:
      name: gitlab.bigbang.dev

    registry:
      name: registry.bigbang.dev
